object SeparateTheNumbers extends App{

    def getFirstBeautifulNumber(x: String) = {
        (1 to x.size/2).
    }

    def isBeautifulNumber(prevNumber:BigInt, rest: String) = {
        if
    }

    val in = io.Source.stdin.getLines()

    for(_ <- 1 to in.next.toInt){
        println(getFirstBeautifulNumber(in.next))
    }
}